{
  "kind": "collectionType",
  "collectionName": "empresas_clientes",
  "info": {
    "singularName": "empresa-cliente",
    "pluralName": "empresas-clientes",
    "displayName": "Empresa Cliente",
    "description": "Empresas que confían en Foton — logos para la sección del home"
  },
  "options": {
    "draftAndPublish": false
  },
  "attributes": {
    "nombre": {
      "type": "string",
      "required": true,
      "minLength": 2,
      "maxLength": 200,
      "description": "Nombre de la empresa (se usa también como alt text si no se especifica)"
    },
    "logo": {
      "type": "media",
      "multiple": false,
      "required": true,
      "allowedTypes": ["images"],
      "description": "Logo de la empresa — usar Media Library. Preferir SVG o PNG transparente."
    },
    "altText": {
      "type": "string",
      "maxLength": 200,
      "description": "Texto alternativo para accesibilidad. Si se omite, se usa el nombre."
    },
    "url": {
      "type": "string",
      "maxLength": 500,
      "description": "URL opcional del sitio de la empresa (se abre en nueva pestaña)"
    },
    "orden": {
      "type": "integer",
      "min": 0,
      "default": 0,
      "description": "Orden de aparición. Menor número aparece primero."
    },
    "activo": {
      "type": "boolean",
      "default": true,
      "description": "Si está inactivo, no se muestra en el sitio"
    }
  }
}
